name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on:
      name: 120
    steps:
      - uses: actions/checkout@v2
      - name: install
        run: yarn

      - name: build
        run: |
          yarn build
